// Inicialización de scripts y animaciones
document.addEventListener('DOMContentLoaded', () => {
    // Iniciar AOS (Animate On Scroll)
    AOS.init({
        once: true,
        offset: 100,
        duration: 800,
        easing: 'ease-out-cubic',
    });
});
